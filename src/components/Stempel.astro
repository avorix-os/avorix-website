---
interface Props {
  class?: string;
}
const { class: className = '' } = Astro.props;
---
<span class={`stempel${className ? ' ' + className : ''}`}>
  <slot />
</span>

<style>
.stempel {
  display: inline-block;
  border: 1.5px solid var(--akzent);
  color: var(--akzent);
  font-family: var(--f-mono);
  font-size: 12px;
  font-weight: 700;
  text-transform: uppercase;
  letter-spacing: .14em;
  /* C6 (Anweisung 23): 14px lag neben der Skala. 16 sprengt die Komponente
     nicht, der Stempel wird nur 4px breiter -- also 16. */
  padding: 8px 16px;
  border-radius: 3px;
  background: var(--flaeche);
  transform: rotate(-1deg);
  transition: transform .18s;
}
.stempel:hover {
  transform: rotate(0deg);
}
.stempel.stempel--hell {
  border-color: var(--akzent-hell);
  color: var(--akzent-hell);
  background: transparent;
}
@media (prefers-reduced-motion: reduce) {
  .stempel {
    transform: none;
    transition: none;
  }
}
</style>
